# Schritt 1: ZIP-Datei herunterladen
wget -O meins.zip https://github.com/ribekagmbh/.sh/archive/refs/heads/main.zip

# Schritt 2: ZIP-Datei entpacken
unzip meins.zip

# meins.zip löschen
rm meins.zip

# Schritt 3: Verzeichnis umbenennen
cp -r .sh-main/docker svws-umgebung
rm -rf .sh-main

# Schritt 4: In das umbenannte Verzeichnis wechseln
cd svws-umgebung

# Schritt 5: Skript ausführbar machen
chmod +x createSvwsServer.sh

# Schritt 6: Skript mit sudo ausführen
sudo ./createSvwsServer.sh

